server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Hymin2/Webtoon-review
          search-paths: config-file/**
          default-label: dev